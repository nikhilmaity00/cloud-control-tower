environments:
  allowed:
    - dev
    - test
    - prod

replicas:
  min:
    allowed:
      - 0
      - 1

  max:
    allowed:
      - 0
      - 1
      - 2

application:
  name:
    pattern: "^[a-z0-9][a-z0-9-]{2,29}$"